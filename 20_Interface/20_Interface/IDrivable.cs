namespace _20_Interface
{
    // Any class that implements the interface must implement all the members of the interface
    interface IDrivable
    {
        void Move();
        void Stop();
    }
}
